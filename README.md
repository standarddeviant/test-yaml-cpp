

# `test-yaml-cpp`

This is a (very) simple set of build scripts to experiment with `yaml-cpp`.

## how to run test program, `test-yaml-cpp`

```
git clone https://github.com/standarddeviant/test-yaml-cpp.git
cd test-yaml-cpp
./yolo.sh
```

